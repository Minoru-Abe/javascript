jsファイルは外だしするのが多分普通

jsファイルの最初に、'use strict'を書くのが推奨、厳格にエラーチェックする、らしい。


エスケープシーケンス
バックスラッシュでエスケープする。

変数はlet(昔はvar)
定数はconst


動的型付け言語

文字列のテンプレートリテラルはバッククォートを使う
変数は${}で

インクリメントは++
デクリメントは--

あとは+=, -=, *=, /=

前置演算子はインクリメント・デクリメントしてから代入
後置演算子は代入してから計算


==は抽象的な比較
===は厳格な比較

三項演算子

(式) ? A : B


関数リテラル（無名関数、匿名関数） 

Functionコンストラクタ